# dday-static — TODO

> 이 폴더는 `public/` 밖이라 배포되지 않는다.
> `wrangler.toml` 의 `[assets] directory = "./public"` 이 업로드 범위를 정하므로,
> 여기 무엇을 넣든 사이트 URL 로는 접근되지 않는다.

> **순위·클릭 관련 항목은 `seo-todo.md` 에 따로 있다.**
> 이 문서는 *"자료를 하나 더 붙일까"* 를, 저쪽은 *"이미 담은 것을 어떻게 찾게 할까"* 를 정한다.
> 게이트 넷은 저쪽에 적용되지 않는다 — 새로 받아 오는 자료가 없다.

## 게이트 — 자료 하나를 붙일지 정하는 기준

황금연휴를 붙이면서 정리했고 천문에서 한 번 더 확인된 넷이다. **넷을 다 통과해야 착수한다.**

1. **미래 날짜가 미리 확정되는가.** 갱신이 월 1회다. 자주 바뀌는 자료를 담으면
   "D-3" 이라고 띄워 놓고 실제로는 밀린 상태가 된다. 사이트가 조용히 거짓말을 한다.
2. **축이 맞는가.** 자리가 셋이다 — 국가축(204개 국가 페이지) · 전세계 공통축(첫 화면·하늘) ·
   이름축(/holiday/, 공휴일 이름 60가지). 셋 다 아니면 놓을 자리가 없다.
   **셋째 자리는 2026-09-03 에 생겼다** — 그전까지 둘뿐이라고 적어 두었는데, 이미 담긴
   자료를 다른 축으로 뒤집으면 자리가 하나 더 나온다는 것을 그때 알았다.
3. **키 없이 받을 수 있는가.** 키가 필요하면 빌드 타임에만 쓰고 Actions secret 에 둔다
   (생성물만 배포되므로 클라이언트에는 안 나간다). 다만 그때부터 갱신이 남의 사정을 탄다.
4. **검산점이 있는가.** ← **이게 실제로 갈랐다.**
   자료가 스스로 앞뒤가 맞는지 볼 방법이 있어야 한다. 황금연휴는 *"이 연휴가 우리가 담은
   공휴일에 걸려 있나"* 가 검산점이었고, 천문은 *"일본이 공휴일로 정한 분점"* 과
   *"음력 명절이 가리키는 삭"* 이었다. 그게 없으면 `check-pages` 가 볼 것이 "받은 대로
   찍혔나" 뿐인데, 그건 아무것도 검사하지 않는 것과 같다.

   **덧 — 착수 전에 4번부터 잰다.** 국제 기념일과 스포츠 대회를 이 순서로 걸러 냈다.
   짓고 나서 "검산할 게 없네" 를 발견하면 이미 늦다. 재는 데 반나절, 짓는 데 이틀이다.

   **덧 — 검산점은 종류도 봐야 한다.** 천문에서 21개 지점을 다 맞히고도 ΔT 를 통째로
   빼먹은 것이 안 잡혔다. 공휴일 검산점이 *날짜*만 주기 때문이다. 자료가 날짜보다 잘게
   나뉘면(시각·수량) 그 단위로 견줄 점이 따로 있어야 한다.

## 완료

- [x] **지구본을 좁은 화면에도 낸다** `shared/globe.js` (2026-09-09)

  > 위 지구본 항목의 **「모바일은 0바이트다」를 뒤집는 항목이다.** 그때 감춘 까닭은
  > 두 가지였다 — 자리가 없다(여백에 걸치는 물건이다), 그리고 잃는 것이 없다
  > (담은 것이 아래 국가 목록과 완전히 겹친다). 두 번째는 지금도 맞다.
  > 첫 번째만 풀면 되는 것이었다.
  >
  > **접어 두는 것으로 0바이트를 지켰다.** 국가 목록 위에 「지구본으로 고르기」
  > 버튼만 두고, 누르는 그 순간에 `globe.json` 을 받는다. 안 누르면 모바일
  > 트래픽이 여전히 0 이다 — 감추기와 달리 원하면 볼 수 있다는 것만 다르다.
  > 자리는 검색창과 목록 사이다. 검색이 먼저고 지구본은 그 다음 손잡이다.
  >
  > **잰 값** — `globe.json` 13,798B (gzip 13.5KB). 위 항목이 적은 3.9KB 는
  > 대륙 윤곽을 넣기 전 값이라 낡았다. `globe.js` 의 주석에도 그렇게 적혀 있었다.
  > 이 값이 「미리 받지 않는다」의 유일한 근거이므로 세 곳(코드 주석 · CSS 주석 ·
  > 하니스 메시지)을 지금 값으로 맞췄다.
  >
  > **문턱이 둘에서 셋이 됐다.** `globe.js` 의 `SHOW` 를 `WIDE` 로 고치고,
  > CSS 는 `min-width:1400px`(여백에 걸치는 모습)과 `max-width:1399px`
  > (목록 위로 들어가는 모습) 둘로 갈랐다. `check-pages` 가 셋을 견주고
  > **`max-width` 가 `WIDE − 1` 인 것까지** 본다 — 서로 안 닿으면 그 사이 한 톡은
  > 어느 모습도 아니게 된다.
  >
  > **모습을 CSS 가 정하지 않는다.** `globe.js` 가 켤 때 한 번 정해 `.wide` /
  > `.touch` 를 붙이고, 두 CSS 블록은 각자 제 클래스가 붙었을 때만 듣는다.
  > 폭만 보고 CSS 가 혼자 정하면 판단하는 자리가 둘이 되는데, 자료를 받을지도
  > 같은 갈림길에서 갈리므로 그건 곧 갈라진다. **덤** — 좁게 켠 창을 넓혀도
  > 지구본이 엉뚱한 자리로 날아가지 않는다.
  >
  > **손가락은 마우스가 아니었다. 셋을 새로 지었다.**
  >
  > · **끌기가 아예 안 돌았다.** `e.movementX` 를 쓰고 있었는데 그 값은 손가락
  >   쪽에서 0 으로 오는 브라우저가 있다. 직전 좌표를 붙들어 직접 빼는 것으로
  >   바꿨다 — 마우스에서도 같은 값이고, 사파리에서 오래 없던 값을 안 쓰게 된다.
  > · **확대할 길이 없었다.** `wheel` 뿐이었다. 두 손가락 거리의 비를 그때 배율에
  >   곱하는 `pinch()` 를 넣었다(벌린 만큼 커지고 되돌리면 되돌아온다).
  >   손가락이 둘이 되는 순간 잡아 둔 점을 놓는다 — 벌리다 떼는 것이 나라 이동이
  >   되면 안 된다. 되돌리기는 `dblclick` 이 안 오는 기기가 있어 `retap()`
  >   (300ms · 24px)으로 직접 잡는다. **×1 에서는 안 본다** — 그래야 점을 두 번
  >   눌러도 배율이 안 흔들린다.
  > · **한 번 눌러 옮기면 안 된다.** 손가락에는 hover 가 없어서 눌리는 순간이
  >   곧 이동이다. 소앤틸리스는 ×1 에서 18개국이 한 자리에 겹치므로 그러면
  >   엉뚱한 나라로 뜬다. **한 번 눌러 이름을 띄우고, 그 이름을 눌러 옮긴다.**
  >   두 걸음이 되니 잡히는 반경을 오히려 키울 수 있었다(11 → 22px) — 잘못
  >   잡혀도 이름을 보고 되돌리므로, 넓은 반경은 「못 잡는 일이 없다」는 뜻뿐이다.
  >   문턱도 넉넉하다(`SLOP` 6 → `SLOP_T` 12): 손가락은 가만히 눌러도 움직여서,
  >   마우스 문턱을 그대로 쓰면 누른 것이 죄다 회전으로 넘어간다.
  >
  > **하니스를 넓혔다 — 이 항목에서 실제로 값을 한 일이다.** 지금까지 캔버스 칸은
  > `matchMedia` 가 늘 false 라 통째로 안 돌았고, 그러면 「접힌 채로는 안 받는다」를
  > 볼 방법이 없다. `boot(page, { media })` 로 폭을 흔들고, `#globe` 를 캔버스까지
  > 엮고, 리스너를 버리지 않고 받아 둬 `fire('pointerdown')` 로 손가락을 흉내 낸다.
  > `textContent` 는 넣으면 자식을 지우게 했다(진짜 DOM 이 그렇다 — 안 지우면
  > 이름 링크가 쌓여 무엇이 지금 뜬 것인지 알 수 없다). `#countries li[data-cc]`
  > 204개도 엮었다 — 지구본이 이름과 주소를 **그 목록에서 읽으므로** 없으면 점을
  > 하나도 안 달고, 그러면 누르기 검사가 전부 조용히 지나간다.
  > **`ctx.arc` 는 브라우저처럼 음수 반지름에 던지게 했다** — 아래 고장이 그것
  > 없이는 안 잡혔다.
  >
  > **화면 기하 한 줄을 순수 함수로 내놨다.** `radius(w, dot)` — 하니스가 화면과
  > **같은 식**으로 좌표를 짚어야 「KR 점을 누른다」를 흉내 낼 수 있다. 상수를
  > 검사기에 베껴 적으면 그때부터 둘이 갈라진다.
  >
  > **그래서 배선을 넷 잡는다** — 손가락으로 한 번 누르면 이름만 뜨고 옮기지 않나 ·
  > 마우스는 한 번에 옮기나 · 60px 끌면 같은 자리에서 KR 이 비켜나나(`movementX`
  > 고장이 되돌아오면 여기서 걸린다) · 벌린 뒤 떼는 것이 누른 것으로 안 읽히나.
  >
  > **걸쇠는 배율로 재야 잡혔다.** 마지막 것을 처음엔 「나라가 골라지나」로 짰는데
  > 걸쇠를 빼도 통과했다 — 확대가 시작될 때 잡아 둔 점을 이미 놓으므로
  > (`downAt = -1`) 애초에 아무것도 안 골라진다. 걸쇠가 지키는 것은 선택이 아니라
  > **배율**이었다: 걸쇠가 없으면 두 손가락을 잇달아 떼는 것이 「두 번 누르기」로
  > 읽혀 방금 맞춘 배율이 ×1 로 되돌아간다. 확대한 자리에 나라가 그대로 있는지로
  > 바꿔 재니 물었다. **검사가 통과하는 것과 검사가 무는 것은 다르다.**
  >
  > **그러다 실제로 하나 잡았다.** 펼쳤다 접고 화면을 돌리면(`resize`) 폭 0 을
  > 다시 재서 반지름이 음수가 되고, 다음 프레임의 `ctx.arc` 가 매 프레임 던진다.
  > `size()` 가 폭 0 이면 재지 않고 돌아서게 고쳤다.
  >
  > **덤으로 고친 것 둘** — `draw()` 가 자료 오기 전에 `save()` 뒤에서 돌아서고
  > 있었다(`restore` 를 안 지나 초당 60번 스택이 쌓였다). 그리고 멈춘 지구본을
  > 초당 60번 다시 그리고 있었다 — `dirty` 플래그를 두어 손을 댈 때만 그린다.
  > 데스크톱에서는 눈에 안 보이던 것이고, 손가락으로 보는 화면에서는 배터리다.
  >
  > **문장은 `globe.js` 가 지지 않는다.** 버튼의 두 말은 `data-open` /
  > `data-close`, 안내는 `.globe-hint.w` / `.globe-hint.t` 로 마크업에 있다.
  > `gen-pages` 가 두 말을 다 찍으므로 영어 화면이 한국말로 열리지 않는다.
  > 안내를 한 문장으로 합칠 수 없는 이유는 휠과 두 손가락이 서로 못 하는
  > 일이기 때문이다. `check-pages` 가 두 벌이 다 있는지, 버튼에 쓰인 말이
  > `data-open` 과 같은지 본다(접었다 펼친 뒤 말이 어긋나는 것이 그 고장이다).
  >
  > **보조기술에는 버튼도 감췄다**(`aria-hidden` · `tabindex="-1"`).
  > 닿지 못하는 것을 여는 버튼이 탭 순서에 끼면 그 자체가 막다른 길이다.
  > 같은 나라가 바로 아래 목록에 다 있다는 근거는 그대로다.
  >
  > **일부러 열한 군데를 깨뜨려 보고 다 물었다** — 좁은 화면에서 미리 받게 하기 ·
  > 문턱 갈라 놓기(`max-width` 1399 → 1300) · 버튼을 감춘 채 두기 ·
  > `pinch` 의 범위 자르기 빼기 · 안내 한 벌 지우기 · 캔버스 크기 안 정하기 ·
  > 폭 0 가드 빼기 · 손가락도 한 걸음에 옮기게 하기 · 끌기 델타를 0 으로 하기 ·
  > 마우스도 두 걸음으로 하기 · 확대 걸쇠 빼기.
  > 열한 번째가 처음에 안 물어서 그 검사를 다시 짰다(위).
  >
  > **폰트 락이 두 번 걸렸다.** `codepoints()` 가 `public/` 의 주석까지 훑으므로
  > 새 한글이 버린 조각을 부른다. 「튄다」의 튄 · 「켤 때」의 켤 · 상자 그림 문자
  > `─` 셋이 걸려 세 번 고쳐 썼다 — 폰트를 다시 뜨는 것보다 말을 바꾸는 쪽이 싸다.

- [x] **연간 히트맵** — 국가 페이지 (2026-09-07)

  > **새 자료가 0이다.** 이미 담아 검증하는 `days` 와 `long` 을 12줄(달) × 31칸(날)로
  > 다시 그린 것이 전부다. todo 가 말하는 *"밖을 보지 말고 안을 한 번 더 뒤집기"* 의
  > 네 번째 사례다(이름 축 · 요일 축 · 지구본에 이어).
  >
  > **표를 대신하지 않는다.** 구글이 색인하는 것은 글자이므로 표는 그대로 두고 그
  > 앞에 요약으로 얹는다. 그래서 `aria-hidden` 을 달고 표를 접근 경로로 남긴다 —
  > 지구본과 같은 논리다.
  >
  > **잰 값** — 칸을 365개 다 그린다. 특수한 칸만 그리고 나머지를 띠로 덮으면
  > 원본 16.9KB → 7.1KB 인데 **gzip 을 지나면 1.2KB 대 0.5KB** 다. 격자가 반복적이라
  > 압축이 다 먹는다. 0.7KB 아끼려고 칸 하나하나를 잃을 이유가 없다.
  > `kr` 페이지 gzip **4.0 → 5.9KB**. `<title>` 과 달 이름표까지 넣어 +1.9KB 이고,
  > 인라인이라 요청은 늘지 않는다. 제목은 공휴일·징검다리에만 붙인다 — 평일·주말에
  > 툴팁은 빈 말이다.
  >
  > **지구본과 달리 좁은 화면에도 보인다.** 긴 표를 한 그림으로 접는 것이라 작은
  > 화면에서 값이 더 크다. 표지 연도만 그린다 — 세 해를 다 그리면 값이 세 배인데
  > *"올해 언제 쉬나"* 는 올해 물음이다.
  >
  > ⚠ **주말이 나라마다 다르다. 이 기능에서 사고가 날 가장 유력한 자리다.**
  > 토·일로 굳혀 그리면 바레인·이집트 같은 금·토 나라 8개국이 조용히 틀린다.
  > 요일 축과 같은 `weekendOf()` 를 쓰고, `check-pages` 는 `Intl` 에 **직접 물어**
  > 따로 계산한다. 일부러 토·일로 굳혀 보니 `/bh/` 가 즉시 걸렸다.
  >
  > **검산점은 같은 페이지의 표다.** SVG 의 칸과 표의 줄을 따로 파싱해 공휴일·
  > 징검다리·주말 세 부류를 **양방향으로** 견준다. 여기에 칸 수(365, 윤년은 366) ·
  > 달마다 그 달의 날수 · 제목이 표의 이름과 같은지를 더한다.
  > **408개 페이지 · 칸 148,920개**가 매번 대조된다. 기대값은 `check-pages` 가
  > 다시 계산한다 — `heatmap()` 이나 `weekendOf()` 를 가져오면 둘이 같이 틀릴 수 있다.
  >
  > **오늘 칸은 런타임에 놓는다.** 빌드 타임에 박으면 날이 지나며 거짓말이 된다.
  > 기하(`data-cell`·`data-gap`·`data-x0`·`data-y`)를 svg 에서 읽으므로 여기 숫자를
  > 다시 적지 않는다. `paintCalendar(iso)` 가 날짜를 인자로 받게 만든 것이 요점이다 —
  > 포인터 이벤트처럼 하니스가 못 만드는 것이 아니라, **두 갈림길을 다 때릴 수 있다**
  > (표지 연도 안이면 자리를 잡고, 다른 해나 히트맵이 없는 페이지면 null).
  >
  > **하니스에 구멍이 하나 있었다.** `doc.querySelector('#id')` 가 속성 없는 스텁을
  > 돌려주어 `getAttribute('data-y')` 가 늘 null 이었다. *"배포되는 HTML 그대로를
  > 돌린다"* 는 전제가 거기서 새고 있었고, 오늘 칸이 조용히 안 놓이는 것으로
  > 드러났다. `harness.mjs` 가 id 붙은 태그의 속성을 읽어 스텁에 실어 준다.
  >
  > **검사가 잡은 것 셋 가운데 둘은 검사 자신의 버그였다.** ① 제목을 `esc()` 를
  > 거치지 않은 이름과 견주어 이름에 `&`·`"` 가 든 나라(TR·NZ·PH·VI)가 걸렸다.
  > ② 징검다리 기대값을 **연휴의 시작 연도**로 세어 엘살바도르의 2027-01-01 을
  > 요구했다(연말 연휴가 다음 해에 징검다리를 둔다). 생성기는 옳았다 —
  > 그 집합은 표지 연도의 날짜에만 물어보므로 화면에는 차이가 없었다.
  > 그래도 양쪽을 날짜 기준으로 맞췄다: 집합이 이 표가 보일 수 있는 것만 담는다.
  >
  > **일부러 넷을 깨뜨려 셋이 물었다** — 주말을 토·일로 굳히기(`/bh/`) · 달 길이를
  > 31 로 굳히기(372칸) · 오늘 칸 한 칸 밀기. **징검다리 기준을 되돌리는 것은
  > 안 문다** — 화면에 차이가 없으므로 잡을 것이 없다. 이건 숨기지 않고 적어 둔다.
  >
  > **2026-09-07 에 오늘 쉬는 나라를 빨간 점으로 찍었다.** 색은 `--today` 로,
  > 이 사이트의 유일한 따뜻한 색이고 히트맵의 오늘 칸과 같은 뜻이다. 204개 중에서
  > 찾는 것이라 색만으로는 얇아 반지름도 0.8px 키웠다. 손이 얹힌 점은 크기로만
  > 알리고 색은 빨강을 지킨다 — 가리키는 동안 「오늘 쉰다」가 사라지면 안 된다.
  >
  > **지구본이 자료를 또 받지 않는다.** 첫 화면의 「오늘 공휴일인 나라」 절을
  > 채우는 `dday.js` 가 이미 `data/month/YYYY-MM.json` 을 읽어 계산하므로 그것을
  > `window.GLOBE.mark(codes)` 로 넘겨받는다. 같은 규칙을 두 벌 두면 같은 날에
  > 두 화면이 다른 말을 한다. `mark()` 는 화면 게이트 **앞에** 둔다 — 좁은 화면에서는
  > 그리기가 아예 안 도는데 `dday.js` 는 화면 폭을 모르고 그대로 부른다.
  >
  > **검산점은 네 지점이 한 집합인가다.** `data/month` → `DDAY.todayCodes` →
  > `#tlist` 에 그려진 링크 → `GLOBE.marked()`. 기대값은 월 파일에서 곧바로 뽑아
  > `dday.js` 의 계산을 되풀이하지 않는다. 2026-09-07 은 11곳(AL BG BM BR CA MZ
  > PR PW SZ US VI)이라 검사가 실제로 돌았다.
  >
  > **셋을 깨뜨려 다 물었다** — 지구본에 안 넘기기 · `mark()` 가 첫 나라 빠뜨리기 ·
  > 넘기는 목록 한 곳 줄이기. 빈 날 갈림길(`tell([])`)은 오늘 11곳이라 안 돌았다.

- [x] **지구본 — 첫 화면에서 나라 고르기** `shared/globe.js` (2026-09-07)

  > **국경이 아니라 점을 쓴다. 이 판단이 결과를 다 가른다.** 담긴 204개국에
  > 소국이 많아서 110m 국경으로 칠하면 몰타·싱가포르·나우루가 1px 이하가 되어
  > 클릭이 안 된다. 러시아는 쉽고 몰타는 못 고르는 지구본은 「나라 고르기」를
  > 절반만 하는 것이다. 점으로 두면 204개가 전부 동등하게 잡힌다.
  >
  > **잰 값** — `globe.json` 3,943B (gzip 약 1.7KB). 국경 폴리곤이면 150~250KB 다.
  > 라이브러리 0. 정사영 투영이 세 줄이고 역투영도 그만큼이라 three.js(600KB)를
  > 받아 올 이유가 없었다. 나라 이름과 주소도 자료로 받지 않는다 — 첫 화면에
  > 이미 204개 링크가 `li[data-cc]` 로 깔려 있어 그것을 읽는다(늘어나는 바이트 0,
  > 목록에서 빠진 나라가 지구본에도 저절로 없다).
  >
  > **게이트 4번(검산점)** — 자료를 세 겹에서 받고 **네 번째 레이어로 검산한다.**
  > 110m countries 의 LABEL_X/Y 181개 + 50m tiny_countries + 50m map_units 로
  > 204개를 채우고, 50m countries **폴리곤**으로 점-다각형 판정을 한다.
  > 폴리곤은 검산에만 쓰고 **배포하지 않는다** — 그래서 분쟁 국경 문제가 애초에
  > 생기지 않는다. 189/204 가 제 나라 폴리곤 안에 든다.
  >
  > **거리로는 검산이 안 된다.** 처음에 「가장 가까운 나라가 자기 자신인가」로
  > 짰더니 204개 중 **56개**가 걸렸다. **꼭짓점까지의 거리는 폴리곤까지의 거리가
  > 아니기** 때문이다 — 레소토의 라벨은 제 국경보다 남아프리카 꼭짓점에 가깝다.
  > 점-다각형 판정이 맞는 검사다.
  >
  > **통과 못 하는 15개는 문턱으로 뭉개지 않고 명단으로 고정했다.** 두 종류다 —
  > 검산용 폴리곤이 아예 없는 9개(해외주·속령이 본국에 합쳐져 있다: GP·MQ·GF·
  > BQ·GI·SJ·CC·CX·TK)와 라벨이 일부러 바다에 놓인 6개(NZ 81km · GQ 90km ·
  > TT 18km · VA 1km · SX 4km, 그리고 SC 1121km). SC 는 점이 **알다브라 환초**
  > (세이셸 영토)인데 50m 폴리곤이 외곽 섬을 안 담는 경우다 — 점이 맞고 검산
  > 자료가 불완전한 쪽이다. 명단은 양방향으로 검사된다: 명단에 없는 나라가
  > 실패하면 실패, **명단에 있는데 통과하면 "예외가 낡았다"로 역시 실패**다.
  > 자료가 좋아지면 명단을 줄이라고 생성기가 말해 준다.
  >
  > **자리 — 오버행.** `.wrap` 이 820px 이라 1400px 화면에서 좌우가 290px 씩
  > 빈다. 그 여백에 `position:absolute; left:100%` 로 걸친다. `top` 을 주지 않아
  > 제 정적 위치(카드 위쪽)에 맞고, 흐름에서 빠져 아래 절을 밀지 않는다.
  > **`.wrap` 을 넓히지 않았으므로 머리말·본문 정렬이 그대로다.**
  >
  > **모바일은 0바이트다.** 기본이 `display:none` 이고, `globe.js` 가 **같은
  > 문턱을 보고 자료조차 받지 않는다**(CSS 로만 감추면 받고 안 보여 준다).
  > 감춰도 잃는 것이 없는 이유는 지구본이 담은 것이 아래 국가 목록과 완전히
  > 겹치기 때문이다 — 그래서 모바일 우선 색인에서도 고유 콘텐츠가 사라지지 않고
  > SEO 비용이 0 이다. 같은 까닭으로 보조기술에는 `aria-hidden` 으로 감췄다.
  > 닿지 못하는 위젯을 나란히 만드는 것보다 그게 맞다.
  >
  > **두 문턱이 갈라지는 것을 막는 검사가 이 항목의 핵심이다.** CSS 의 min-width
  > 와 `globe.js` 의 `SHOW` 를 서로 견준다 — 갈라지면 모바일이 3.9KB 를 받고 안
  > 보여 주거나 넓은 화면에서 자리만 잡고 빈다. 그걸 잡을 다른 방법이 없다.
  >
  > **하니스가 캔버스를 못 보므로 구조를 그에 맞췄다.** 계산을 `window.GLOBE` 로
  > 내보내고(`project`·`unproject`·`pick`) `check-pages` 가 그리기를 거치지 않고
  > 직접 때린다 — 보는 방향이 원 중앙(z=1)인가, 대척점이 뒷면(z=−1)인가,
  > 앞면 점 **142개**가 역투영으로 제자리에 돌아오나, 원판 밖이 null 인가,
  > KR 점의 화면 좌표를 누르면 KR 이 잡히나. `window.GLOBE` 가 화면 게이트
  > **앞에서** 붙는 것까지 검사한다 — 뒤로 밀리면 하니스가 계산을 영원히 못 본다.
  >
  > **일부러 다섯 군데를 깨뜨려 보고 다 물었다** — 문턱 갈라 놓기 · z 부호 뒤집기 ·
  > 원판 밖 판정 느슨하게 하기 · `display:none` 지우기 · 자료에서 몰타 빼기.
  >
  > **덤으로 걸린 것** — `globe.json` 이 `data/` 에 생기자 `gen-pages` 와
  > `check-pages` 가 그것을 국가 파일로 읽고 `days` 가 없다며 터졌다. 양쪽의
  > 제외 목록(`SPECIAL` · `NOT_COUNTRY`)에 넣었다. `data/` 에 국가 아닌 파일을
  > 더할 때는 두 곳을 같이 고쳐야 한다.
  >
  > **폰트 조각이 한 글자에 걸렸다.** `codepoints()` 가 `public/` 아래 텍스트를
  > 전문 그대로 훑으므로 주석 글자도 센다. globe.js 가 새로 들인 한글 18자 중
  > **「옅」 하나**가 버려 둔 조각 3개를 불러냈다(Gowun 26 · Pretendard 302·394).
  > 「연하게」로 바꿔 0 으로 만들었다. `public/` 안에 주석을 쓸 때는 그 파일에
  > 없던 글자를 들이지 않는 편이 낫다.
  >
  > **2026-09-07 저녁에 두 가지를 고쳤다.** 실제로 굴려 보고 나온 것들이다.
  >
  > ① **회전하려고 끌었는데 손을 놓는 순간 점이 눌렸다.** `pointerup` 이 무조건
  > 최근접 점을 찾아 이동했기 때문이다. 끌린 거리를 재서 6px 을 넘으면 회전으로
  > 보고, 거기에 **누른 곳과 놓은 곳이 같은 점**인 것까지 본다 — 거리만으로는
  > 빈 자리를 누르고 점 위에서 놓는 경우가 남는다. 판정을 `clickable()` 로 떼어
  > `window.GLOBE` 에 내보냈다. 포인터 이벤트는 하니스가 못 만들지만 순수 함수는
  > 때릴 수 있다 — 소스를 정규식으로 보는 것보다 낫다. 경계값 7건을 검사한다.
  >
  > ② **점만으로는 어느 대륙인지 알기 어려웠다.** `ne_110m_land` 를 Douglas–Peucker
  > 로 0.5° 까지 줄여 해안선을 배경에 깐다. 칠하지 않고 선으로만 긋는다 — 채우려면
  > 링을 지평선에서 잘라 테두리를 따라 이어 붙여야 하고 그 이음이 어긋나면 대륙이
  > 엉뚱한 모양으로 번진다. 격자와 같은 방식(뒷면에서 끊고 다시 시작)이면 그
  > 위험이 아예 없다.
  >
  > **잰 값** — 원본 링 128·꼭짓점 5,143 → 0.5° 에서 링 103·꼭짓점 1,634.
  > `globe.json` 3.9KB → **21.5KB (gzip 8.5KB)**. 1.0° 면 gzip 4KB 로 더 줄지만
  > 1px 넘게 어긋나기 시작한다(240px 지구본에서 1px 이 0.75° 다).
  >
  > **검산점 둘을 더 세웠다.** 손으로 적은 육지·바다 검문(생성기 11개 · check-pages
  > 10개, **서로 다른 지점**)과 **나라 점과의 대조 142/204**. 뒤엣것이 값어치가 있다 —
  > 이미 검증한 점 자료를 새 자료에 대 보는 것이라, 둘이 같은 좌표계에 있지 않으면
  > 142 가 무너진다. 섬나라·소국 62개가 바다로 떨어지는 것은 110m 육지에 표현되지
  > 않기 때문이고 그게 맞다. 점-다각형 판정은 `check-pages` 가 **다시 쓴다** —
  > 생성기 함수를 가져오면 둘이 같이 틀릴 수 있다.
  >
  > **일부러 넷을 깨뜨려 다 물었다** — 끌기 판정 없애기 · 같은 점 조건만 빼기 ·
  > 윤곽의 위경도 맞바꾸기 · 링 절반 지우기.
  >
  > **폰트 조각이 또 한 낱말에 걸렸다.** 새 주석의 「뗄·뗀」이 조각 3개를 불러냈다.
  > 「놓을·놓은」으로 바꿔 0 으로 만들었다. `public/` 안에 주석을 쓸 때마다 이게
  > 걸리는데, 하니스가 매번 잡아 주므로 사고는 안 난다.
  >
  > **2026-09-07 밤에 휠 확대를 붙였다.** 카리브의 소국들이 뭉쳐 클릭이 안 된다는
  > 것이 계기다. 붙이기 전에 **얼마나 확대해야 풀리는지부터 쟀다.**
  >
  > | 배율 | 한 자리에 겹치는 최대 |
  > |---|---|
  > | ×1 | **18개** (소앤틸리스 전체) |
  > | ×2 | 11 |
  > | ×4 | 8 |
  > | ×8 | **4** (AI·BL·MF·SX) |
  > | ×20 | 4 — 평평해진다 |
  >
  > 그래서 **×8 에서 멈춘다.** 더 키워도 얻는 것이 없고 해안선만 거칠어진다.
  > 도미니카·그레나다처럼 처음 문제로 지목된 나라들은 ×4 면 완전히 풀린다.
  >
  > **줌만으로는 끝나지 않는다. 이것을 숨기지 않는다.** MF–SX(생마르탱·신트마르턴)
  > 는 한 섬을 나눠 쓰는 나라라 240px 화면에서 **0.1px** 떨어져 있다. 22px 로
  > 벌리려면 ×167 이 필요하다. AI·BL 까지 넷이 서로 30km 안이다. 남은 넷은
  > 아래 국가 목록으로만 닿는다 — **어느 나라도 닿지 못하는 것은 아니다.**
  > 겹친 자리를 누르면 이름 목록을 펼쳐 고르게 하는 것이 남은 절반이다.
  >
  > **구현은 반지름 한 값이다.** 정사영은 `r` 을 곱해 그리므로 확대는 `r × zoom`
  > 이 전부다. 원판으로 잘라(`ctx.clip`) 창틀 크기는 그대로 두고 안쪽만 커진다.
  > 창틀 원은 자르기 **밖에서** 긋는다 — 안에서 그으면 확대할 때 같이 커진다.
  > 곁들여 고친 것 둘: 격자 간격을 ×4 부터 30°→10° 로 줄이고(그러지 않으면 격자가
  > 화면 밖으로 다 나간다), 드래그 각도를 배율로 나눈다(그러지 않으면 ×8 에서
  > 손을 조금만 움직여도 지구가 달아난다).
  >
  > **`pick` 을 `tied` 위에 얹었다.** 겹친 점 전부를 가까운 순으로 주는 함수가
  > 밑이고 `pick` 은 그 첫 칸이다. 같은 계산을 두 벌 두지 않으려는 것이고,
  > 겹침 수를 검사할 수 있게 된 것이 덤이다 — `check-pages` 가 카리브 한가운데를
  > 눌러 보고 **17→1** 로 줄어드는 것을 본다. 배율 자르기·휠 방향·정렬 순서·
  > 뒷면 걸러내기·MAX_ZOOM 범위 다섯을 깨뜨려 보고 다 물었다.
  >
  > **해안선을 0.5°→0.2° 로 다시 줄였다.** ×8 에서 1px 이 0.094° 이므로 0.5° 면
  > 5.3px 어긋나 눈에 보인다. 0.2° 는 2.1px 다. `globe.json` gzip 8.5→13.7KB —
  > 5KB 를 더 쓴다. 확대해 놓고 해안선이 5px 어긋나 있으면 확대가 쓸모없어지므로
  > 1400px 이상에서만 받는 자료에 그만큼은 쓴다. 좌표는 소수 첫째 자리로 굳혔다
  > (0.1° 가 ×8 에서 1.1px 이라 더 잘게 담아도 화면에 나오지 않는다).
  >
  > **육지 대조가 142→147 로 바뀌었고 `check-pages` 쪽 상수를 안 고쳐서 실패했다.**
  > 두 곳에 같은 수를 적어 두는 설계가 의도대로 문 것이다 — 한쪽만 고치면 멈춘다.

- [x] **이름 축 + 세계가 함께 쉬는 날** `/holiday/` (2026-09-03)

  > **밖에서 받아온 것이 하나도 없고 `data/` 에 파일도 하나도 더 만들지 않았다.**
  > 이미 담아서 검증하는 204개국 공휴일을 **이름**으로 다시 묶은 것이 전부다.
  > 페이지 418 → 540 (허브 둘 + 이름 60 × 2언어).
  >
  > **잰 값** (표지 연도 2026, `e ?? n` 을 정규화한 이름 기준):
  > 문턱 5개국을 넘는 이름 **60가지** · 3년치 (국가, 날짜) 쌍 5,359건 · 날짜 행 1,152개.
  > 새해 194개국 · 크리스마스 178 · 노동절 130 · 성금요일 128 · 부활절 월요일 126 ·
  > 독립기념일 104(날짜는 **239가지**) · 성 스테파노 축일 74.
  >
  > **두 종류로 갈린다. 이것이 이 축의 값어치다.** 크리스마스는 178개국이 거의 한 날에
  > 쉬고, 독립기념일은 104개국이 저마다 다른 날 쉰다. 어느 쪽도 국가 페이지로는 답이
  > 나오지 않는다 — 204장을 다 열어야 하는 물음은 답이 없는 물음과 같다.
  >
  > **검산점이 `data/month/*.json` 보다 세다.** 저쪽은 생성기가 파생물을 저장해 두고
  > 견주는데, 여기는 파생물이 없다. `gen-pages` 가 그때그때 묶어 HTML 에 박고
  > `check-pages` 가 **자기 손으로 다시 묶어** 그 HTML 과 견준다 — 묶는 코드를 나눠
  > 쓰지 않으므로 둘이 같이 틀릴 수가 없다. 칩 앞에 적힌 수와 실제 칩 개수, 칩이
  > 보이는 이름순인지까지 본다.
  >
  > **표기 정규화는 규칙이고 뜻으로 묶는 것은 의견이다.** `All Saints' Day` ·
  > `All Saints Day` · `All Saints’ Day` 로 51개국이 세 갈래로 쪼개져 있었다 —
  > 소문자·아포스트로피·구두점만 걷어 기계적으로 묶었다. `Labour Day` 와
  > `Workers' Day` 와 `May Day` 는 묶지 않았다. 묶으면 노동절 하나로 160개국이 되지만
  > 미국 Labor Day 는 9월이고 영국 May Day 는 노동절이 아니다 — 그 판단이 어디서도
  > 검사되지 않는다.
  >
  > **국제 기념일이 죽은 이유가 여기서는 안 걸린다.** 저쪽은 한국어 라벨이 166/554
  > 였고 **사실 자체가 손으로 적은 것**이었다. 여기서 손으로 적는 것은 라벨과 슬러그
  > 60쌍뿐이고(`tools/holiday-names.mjs`), 어느 나라가 언제 쉬는가는 전부 파생된다.
  > 라벨이 틀리면 화면에 보이고, 문턱을 새로 넘는 이름에 라벨이 없으면 `gen-pages` 가
  > 종료 코드 1 로 멈춘다.
  >
  > **「세계가 함께 쉬는 날」(옛 A급)은 이 축의 부산물로 나왔다.** 같은 자료를 이름이
  > 아니라 **날짜**로 뒤집으면 되고, 허브의 표가 그것이다 — 2026년에 10개국 이상이
  > 함께 쉬는 날 **44일**(새해 197개국 · 크리스마스 178 · 노동절 159 · 부활절 월요일 120).
  > 따로 지을 일이 아니었다.

- [x] **나라끼리 견주기** `/rank/` (2026-09-03)

  > 한 장이다. 표가 넷(최장 연휴 · 공휴일 많은 나라 · 적은 나라 · 연휴 많은 나라)이고
  > 전부 표지 연도 상위 20줄이다. 이것도 자료를 하나도 더 만들지 않는다.
  >
  > 2026년: 공휴일 최다 스페인 29일 · 엘살바도르 25 ↔ 최소 지부티 3 · 베트남 4.
  > 최장 연휴 엘살바도르 9일(8월 1일 · 11월 7일) · 루마니아·러시아 7일.
  >
  > **세는 단위를 국가 페이지와 다르게 두고 각주로 밝혔다.** 국가 페이지 요약은
  > 자료 **건수**를 세는데(대한민국 2025-05-05 은 어린이날과 부처님 오신 날이 겹친다)
  > 순위에서 그러면 "쉬는 날" 이 부풀려진다. 여기서는 **날짜 수**를 세고 열 이름을
  > "쉬는 날짜" 로 두었다. 두 페이지가 같은 수를 다르게 세는 것은 피할 수 없으니
  > 숨기지 말고 적는다.
  >
  > **최장 연휴 표는 `tr data-s/data-e` 를 그대로 쓴다.** 국가 페이지의 연휴 표와
  > 같은 `classifyBreaks()` 가 D-day 를 붙인다 — 여기서 규칙을 새로 만들면 사이트
  > 안에서 D-day 가 두 가지 뜻을 갖는다.
  >
  > **뺀 지표 하나 — 나중에 되살렸다.** "주말에 겹쳐 날아간 공휴일" 은 2026년
  > (국가, 날짜) 2,500쌍 중 540(21.6%)이었는데 그건 **토·일 고정으로 센 값**이라
  > 이집트(금·토)에서 조용히 틀린다. 주말이 어느 요일인지가 우리 자료에 없어서 뺐다.
  >
  > **2026-09-04 에 요일 축(`/weekday/`)에서 되살렸다** — 그 자료가 `Intl` 에 있었다
  > (`Intl.Locale('und-EG').getWeekInfo().weekend` 가 `[5,6]` 을 준다. 204개국 전부).
  > 나라별 주말로 세니 540 이 아니라 **544(21.8%)** 다.
  > 이 문단이 그때 잘못 넣는 것을 막아 줬다.

- [x] **음력 달력** `/sky/lunar/` (2026-09-03)

  > 하늘의 네 번째 갈래. 3년치 **37개월**(윤달 1개 — 2025년 윤6월).
  > **새 이론도 네트워크도 없다** — 이미 있는 삭(Meeus 49장)과 중기(VSOP87D) 둘이
  > 규칙의 전부다. 초하루는 삭이 든 날, 동지가 든 달은 11월, 윤달은 중기가 들지
  > 않는 첫 달(무중치윤법). `astro.mjs` 에 `lunarMonths()` 가 붙었다.
  >
  > **시간대가 자료의 일부인 첫 자료다.** 절기·삭망은 온 세계가 같은 순간을 공유하고
  > 갈리는 것은 날짜뿐이라 `sky.json` 이 kst·utc 두 칸을 담았다. 음력은 *"삭이 든
  > 날이 며칟날인가"* 로 달이 갈리므로 기준 시간대를 고르지 않으면 달력이 성립하지
  > 않는다. KST 한 벌만 담고 en 페이지도 같은 표를 본다 — 그래서 이 갈래만
  > `note`·`foot` 를 따로 갖는다(다른 갈래의 "이 페이지는 UTC 기준" 을 그대로 쓰면
  > en 페이지가 거짓말한다).
  >
  > **검산점 18개.** 규칙 셋을 저장소 안에서 되짚고(초하루↔삭 · 동지↔11월 ·
  > 무중치윤법), 그 위에 Nager 공휴일로 밖에서 한 번 더 본다 — 추석(음력 8월 15일)과
  > 홍콩 中秋節翌日(8월 16일)은 3년 다 **정확 일치**, 설날은 **연휴 구간 내**
  > (Nager 가 일요일에 걸린 날을 빼고 주는 나라가 있다 — 2027 KR 설날),
  > 중국 春节·홍콩 農曆年初一 은 **하루까지 허용**한다.
  >
  > **그 하루는 오차가 아니라 사실이다.** 중국 농력은 UTC+8 기준이라 삭이 두 자정
  > 사이에 떨어지는 해에 한국 음력과 하루 갈린다 — 2027년이 그렇다(삭 2027-02-06
  > 15:56 UTC → 중국 2월 6일 23:56 · 한국 2월 7일 00:56). 이걸 오차로 보고 맞추려
  > 들면 달력을 틀리게 만든다.
  >
  > **일부러 깨뜨려 보고 두 번 뚫렸다.** ① `covered` 를 `+isoAt(e-1)` 로 재서
  > `NaN` 이 되었고, 그래서 중기 검사가 통째로 잠들어 있었다 — 윤달 표시를 지워도
  > 통과했다. `.slice(0,4)` 를 빠뜨린 것뿐이다. ② 달 번호를 통째로 미는 고장은
  > 중기 검사로도 안 잡힌다(밀린 달에도 중기는 하나씩 들어 있다). 그래서
  > *"한 음력 해의 평달 번호는 1..12 가 한 번씩"* 을 따로 걸었다.

- [x] **황금연휴** — Nager `LongWeekend` (2026-08-28)

  > 204개국 · 3년치 · 4,043건. 길이 분포는 3일 2,117 · 4일 1,741 · 5일 153 · 6일 이상 32.
  > 연휴가 0건인 국가는 없었다.
  >
  > **검산점이 된 것** — Nager 의 LongWeekend 는 `types` 를 가리지 않는데 우리 표에는
  > `Public` 만 있다. 그래서 *"우리가 담은 공휴일에 걸리지 않은 연휴는 버린다"* 를 규칙으로
  > 두고, `check-pages` 가 남은 것 전부에 대해 그 규칙을 다시 검사한다. 이번 갱신에서는
  > 0건이 걸렸지만 미국·브라질처럼 `Public` 아닌 항목이 섞인 나라가 있어 언젠가는 걸린다.
  >
  > **일수를 자료에 담지 않았다.** `s`·`e` 에서 나오므로 담아 두면 두 값이 갈라질 수 있고,
  > 그러면 그것까지 검사해야 한다.
  >
  > **갈림길이 셋이라 `classify()` 를 못 썼다** — 연휴는 하루가 아니라 구간이다.
  > 시작 전 `D-12` · 그 사이 `연휴 중` · 끝난 뒤 `D+3`. 카드의 "다음 연휴" 는 지금 붙어
  > 있는 연휴가 있으면 그것이다(`classifyBreaks()`).
  >
  > **덤으로 고친 것** — `getJson` 이 "200 인데 본문이 빈" 응답을 404 처럼 다룬다.
  > LongWeekend 가 실제로 그렇게 돌려주는 국가가 있고, 기존 코드였으면 3번 재시도 후
  > 전체 갱신이 죽었다.

- [x] **천문 — 24절기 · 삭망 · 유성우** `/sky/` (2026-08-28)

  > 절기 72 · 삭망 74 · 유성우 30. 페이지 410 → 412 (`/sky/` · `/en/sky/`).
  >
  > **자료가 한 벌이다.** 전 세계가 같은 순간을 공유하므로 `data/sky.json` 21KB 하나다.
  > 그 대신 **"첫 화면 아니면 전부 국가 페이지" 가정을 깨야 했다** — `config.mjs` 의
  > `EXTRA` 가 그것이고, `check-pages` 가 갈래를 셋으로 나눈다.
  >
  > **게이트 4번(검산점)이 이 항목의 전부였다.** 우리가 직접 계산한 자료라 "받은 대로
  > 찍혔나" 로는 아무것도 검사되지 않는다. 두 번째 점을 **저장소 안에서** 찾았다 —
  > 일본은 `春分の日`·`秋分の日` 를 실제 천문 분점으로 정하고, 음력 1월 1일은 삭이 있는
  > 날, 음력 8월 15일은 그 14일 뒤다. Nager 자료와 **21개 지점**을 견준다.
  > 대체공휴일이 있는 명절은 뺐다 — 계산일이 일요일이면 월요일로 밀려서 검산점이 아니라
  > 요일 검사가 된다(부처님오신날 2026 이 그랬다).
  >
  > **정밀도 때문에 태양 이론을 갈아엎었다.** Meeus 저정밀식(0.01° ≈ 15분)으로 짰더니
  > **2025년 동지가 KST 자정 정각**에 떨어졌다 — 12월 21일인지 22일인지 정할 수 없었다.
  > VSOP87D 정본(CDS VI/81)을 받아 172항으로 절단하니 오차가 **11분 → 1분**이 되었다.
  > 계수는 기억으로 적지 않고 `tools/gen-vsop87.mjs` 가 받아 만든다.
  >
  > **유성우는 날짜를 손으로 적지 않았다.** 국제유성기구가 극대기를 태양 황경으로
  > 정의하므로, 검증된 태양 계산에 λ☉ 를 넣으면 해마다 맞는 날짜가 나온다.
  > 애초에 "검산점 없음" 으로 경고했던 부분인데, 손 표를 날짜가 아니라 **황경** 표로
  > 바꾸니 그 문제가 절반은 사라졌다. 남은 위험은 λ☉ 오타뿐이고 그건 "알려진 달·일에서
  > 사흘 안" 으로만 막는다 — 정밀 검산이 아니라 오타 방지다.
  >
  > **시간대 전제는 다르게 풀었다.** 「기존 것 보강」의 국가별 시간대가 전제라고 적어
  > 두었는데, 그러지 않았다. ko 페이지는 KST, en 페이지는 UTC 로 굳혀 박고 각주로
  > 밝힌다. 절기가 애초에 동아시아 개념이라 한국어 독자에겐 KST 가 맞고, 그 덕에
  > 시간대 항목을 기다리지 않고 갈 수 있었다. **시간대 항목은 여전히 유효하다** —
  > 그건 국가 페이지의 "오늘" 문제지 하늘 페이지 문제가 아니었다.
  >
  > **검사에서 두 번 뚫렸다.**
  > · `TERM_NAMES` 로 만들고 `TERM_NAMES` 로 검사해서 **동지와 하지를 맞바꿔도 통과**했다.
  >   이 저장소가 늘 경계하던 바로 그 함정에 그대로 빠졌다. check-pages 에 이름표를 따로
  >   적고 **드는 달**까지 본다 — 동지가 6월에 뜨는 것은 못 숨긴다.
  > · 공휴일 검산점은 **날짜**만 주므로 ΔT(75초)를 통째로 빼먹어도 조용했다. 순간을 분에서
  >   자른 것도 겹쳤다. 초까지 담고 `sky-fixture.mjs` 의 공표 시각 12건과 분 단위로 견준다.

## 다음 후보

### A급 — 게이트 넷을 다 통과했다

- [ ] **식(蝕) — 일식 · 월식** `/sky/eclipse/` (2026-09-07 조사)

  > **밖에서 받아올 것이 하나도 없다.** `astro.mjs` 의 `phaseJde` 가 삭망을 내려고
  > 이미 계산하는 `k · T · E · M · M′ · F · Ω` 가 Meeus 54장(식)이 요구하는 인자와
  > **같다.** 판정은 그 값에 식 조건을 대는 것뿐이다. 파일도 네트워크도 키도 늘지 않는다.
  > 위에 적어 둔 *"밖을 먼저 보지 말고 안을 한 번 더 뒤집어 볼 것"* 의 세 번째 사례이며,
  > 앞의 둘(이름 축 · 요일 축)이 **자료**를 뒤집은 것이라면 이건 **계산**을 뒤집은 것이다.

  **게이트**

  1. **확정성** — 천체역학이다. 수천 년 앞까지 확정이라 갱신 주기와 아예 무관하다.
     지금까지 담은 어느 자료보다 이 항목이 세다.
  2. **축** — 전세계 공통축. 공휴일은 나라마다 다르고 절기·삭망조차 시간대마다 날짜가
     갈리는데, **식은 하나의 사건이다.** `2026-08-12` 는 어디서 보든 `2026-08-12` 다.
     공통축에 이만큼 순수하게 맞는 자료가 없었다.
  3. **무키** — 계산이다.
  4. **검산점 — 재 봤다. 문다.** 착수 전에 4번부터 잰다는 원칙대로, 짓기 전에 확인했다.

  **검산점 넷.** 종류가 갈리는 것이 중요하다 — 천문에서 ΔT 를 통째로 빼먹고도 안 잡힌
  것은 공휴일 검산점이 *날짜*만 주었기 때문이다. 식은 **날짜 · 시각 · 수량** 셋 다 준다.

  - **삭망 표와의 짝** (날짜+시각) — 일식은 반드시 삭에, 월식은 반드시 망에 일어난다.
    이미 검증된 `sky.json` 의 `moons` 74건에서 짝이 안 나오면 계산이 틀린 것이다.
    2025~2027 의 식 12건이 **전부** 짝을 찾았다.
  - **γ 와 종류의 정합** (수량) — `|γ| < 0.9972` 면 중심식, `0.9972~1.5433+u` 면 부분,
    그 밖이면 식이 아니다. 종류와 γ 가 서로 어긋나면 어느 한쪽이 틀렸다.
  - **사로스 주기** (간격) — 같은 계열은 6585.3213일 뒤에 되풀이된다. 창을 앞뒤로
    넓혀 계산하면 계열별 간격이 이 값과 맞아야 한다. **ΔT 를 빼먹으면 여기서 물린다** —
    시각 단위 검산점이라 날짜만 보는 눈에는 안 걸리던 종류의 실수를 잡는다.
  - **식의 계절** (군집) — 식은 173.31일 간격으로 무리 지어 온다. 12건이 여섯 무리로
    갈리는지 본다.

  **잰 값** (2025~2027, Meeus 54 조건을 `sky.json` 의 삭망 표에 붙여 실측)

  식 **12건** — 일식 6 · 월식 6. 12건 전부 삭망 표에서 짝을 찾았다.

  | 날짜(UTC) | | 종류 | γ |
  |---|---|---|---|
  | 2025-03-14 | 월식 | 개기 | 0.350 |
  | 2025-03-29 | 일식 | 부분 | 1.042 |
  | 2025-09-07 | 월식 | 개기 | −0.276 |
  | 2025-09-21 | 일식 | 부분 | −1.064 |
  | 2026-02-17 | 일식 | 금환 | −0.974 |
  | 2026-03-03 | 월식 | 개기 | −0.377 |
  | **2026-08-12** | 일식 | **개기** | 0.899 |
  | 2026-08-28 | 월식 | 부분 | 0.499 |
  | 2027-02-06 | 일식 | 금환 | −0.296 |
  | 2027-02-20 | 월식 | 반영 | −1.052 |
  | **2027-08-02** | 일식 | **개기** | **0.142** |
  | 2027-08-17 | 월식 | 반영 | 1.284 |

  **표지 연도가 마침 좋다.** 자료 창 안에 큰 개기일식이 둘 있다 — 2026-08-12(그린란드·
  아이슬란드·스페인)와 2027-08-02. 뒤엣것은 γ 가 0.142 로 그림자 중심에 바짝 붙어
  개기가 6분대다. 두 해 다 표지 연도를 지나기 전에 온다.

  **범위를 그어야 한다.** *"어디서 몇 시에 몇 퍼센트 가려지나"* 는 Besselian elements 가
  필요해 무게가 다르다. **날짜 · 종류 · 최대식 시각(UTC/KST) · γ · 크기 · 대략적 관측
  지역**까지로 끊으면 계산만으로 끝난다. 지역은 γ 와 계절에서 위도대까지만 말하고
  나라 이름은 적지 않는다 — 적는 순간 검산점이 없는 자료가 된다.

  **자리** — `/sky/` 아래 여섯 번째 갈래다. 허브의 카드 줄과 첫 화면 하늘 칸이 지금
  넷(절기·삭·보름·유성우)인데, 식을 넣으면 다섯이 된다. `check-pages` 의
  *"하늘 칸이 4줄"* 검사가 그 수를 박아 두었으므로 같이 고쳐야 한다.

_여기 있던 「세계가 함께 쉬는 날」은 이름 축을 지으면서 그 부산물로 나왔다 —
위 「완료」 참고. 따로 지을 일이 아니었다._

**그때 배운 것 하나.** 이 문서는 여태 *"자료를 하나 더 받아올까"* 만 물었는데,
값어치 있는 후보 셋이 잇달아 **이미 담긴 자료를 다른 축으로 뒤집는 것**에서 나왔다
(이름 축 · 날짜 축 · 순위). 게이트 넷 가운데 3(무키)과 4(검산점)를 자동으로 통과하고,
1(확정성)도 원본 자료에서 물려받는다. **다음에도 밖을 먼저 보지 말고 안을 한 번 더 뒤집어 볼 것.**
남은 축 후보는 아래 「기존 것 보강」의 시간대뿐이고, 그건 자료가 아니라 계산이다.

**2026-09-04 에 그 뒤집기에 한 겹이 더 있는 것을 알았다.** *"우리가 담은 자료"* 만
뒤집을 게 아니라 **런타임이 이미 들고 있는 표**도 자료다 — `Intl` 의 `getWeekInfo()`
(요일 축의 나라별 주말)와 `-u-ca-` 달력 표(`/sky/calendar/`)가 그렇게 나왔다.
파일도 네트워크도 키도 늘지 않고, 게이트 3(무키)을 공짜로 통과한다. 대신 **받아 적은
값이라 검산점을 따로 세워야 한다** — 우리 계산과 견주거나, 손으로 적은 고정값과 견주거나,
같은 표에 다른 질문을 던지는 식이다. 다음에도 밖을 보기 전에 `Intl` 을 먼저 뒤져 볼 것.

### B급 — 재료는 있는데 검산점이 약하다

_지금은 비어 있다. 여기 있던 국제 기념일과 스포츠 대회는 재 보고 아래 「탈락」으로 갔다._

### 보류 — 막힌 지점이 저마다 다르다

- [ ] **영화 개봉일** — TMDB. **딱 하나만 재면 판정된다** (2026-08-28 조사)

  > 처음에 하고 싶었던 것이고, **유료라서 못 하는 게 아니다.** 무료 등급이 있고 키 발급도
  > 무료다. 이 구조에서는 키가 클라이언트에 나가지도 않는다 — `gen-holidays.mjs` 처럼
  > 빌드 타임에만 부르고 Actions secret 에 넣으면 된다.
  >
  > **게이트 1번은 풀렸다 (사용자 판단, 2026-08-28).** 개봉일이 밀리는 것은
  > *"개봉일은 밀릴 수 있습니다"* 라고 페이지에 적어 넘어간다. 아래에 적어 두었던
  > "월 1회 갱신이면 거짓말이 된다" 는 그 표기로 해소된다.
  >
  > **축은 정확히 맞는다.** `/movie/{id}/release_dates` 는 국가별(ISO 3166-1)로
  > 개봉일 + **개봉 형태 6종**(1 프리미어 · 2 제한개봉 · 3 와이드 · 4 디지털 · 5 물리 ·
  > 6 TV) + 등급을 준다. 204개 국가 페이지에 "이 나라 다음 개봉작" 을 얹을 수 있다는 뜻이고,
  > 그러면 지금까지 붙인 것 중 가장 풍부한 자료가 된다.
  >
  > **⚠ 남은 물음은 하나다 — 예정작에 국가별 날짜가 실제로 채워져 있나.**
  > TMDB 문서에 명시가 없고, 공개 웹페이지는 JS 렌더라 못 읽었다. 무료 키 하나면
  > 예정작 20~30편의 국가별 커버리지를 재서 30초 안에 판정된다. **그것만 하면 된다.**
  >
  > **Wikidata 는 대안이 못 된다 — 재 봤다.** `P577`(발행일) + `P291`(발행 장소) 로
  > 국가별 개봉일이 달려 있긴 한데, 개봉 **후에** 채워진다.
  > · 이미 개봉: Better Man 63개국 · The Substance 54 · Godzilla x Kong 51 · Alien: Romulus 40
  > · 앞으로 개봉: **최대 4개국**, 대부분 2개국
  > D-day 사이트에 필요한 건 정확히 후자라 못 쓴다. (한국어 제목은 15/25 로 기념일보다 낫다.)
  >
  > **게이트 4번은 여전히 열려 있다.** 개봉일은 TMDB 한 곳에서 온 사실이고 견줄 두 번째
  > 점이 없다 — 기념일·스포츠를 죽인 바로 그 문제다. 한 가지 길은 **이미 개봉한 영화로
  > TMDB 와 Wikidata 를 대조**하는 것이다. Wikidata 가 과거 영화에 40~63개국을 갖고 있으니
  > 두 출처가 과거에 대해 일치하면 *파이프라인 자체*는 검증된다. 미래 날짜를 보증하진
  > 못하지만 "받아 적은 게 맞나" 에는 답한다.
  >
  > 그 외 — attribution 로고가 필수고, 상업적 이용은 별도 승인이다.
  > 갱신을 주 1회로 올리면 매주 412개 페이지를 건드리는 커밋이 쌓이고 `lastmod` 가 매주
  > 바뀐다. 국가 페이지에 얹는다면 개봉작 칸만 따로 받는 쪽(클라이언트 fetch)도 생각해 볼 것.

- [ ] **로켓 발사 일정** — Launch Library 2 (무키)

  > 키가 없고 무료라 3번은 통과한다. 재미도 있다.
  > **1번(갱신 주기)에서 영화보다 더 심하게 걸린다.** 영화는 "밀릴 수 있습니다" 표기로
  > 넘어갔지만 이쪽은 그 표기로도 안 된다 — 발사는 하루 단위로 밀린다. 월 1회 갱신으로는
  > 담긴 자료가 거의 항상 틀린 상태가 된다.

### 탈락 — 근거를 남겨 둔다

- [ ] **대형 국제 스포츠 대회** — 기념일과 같은 날 재 보고 내렸다 (2026-08-28)

  > B급 사유가 *"크기"* 였는데, 재 보니 **크기는 생각보다 더 작고 검산점은 아예 없었다.**
  >
  > **① 규모 — 미래 대회 10~12건.** todo 는 "손으로 쓴 30줄" 로 봤는데 실제로는 절반이다.
  > Wikidata 에서 2024~2036 에 시작일이 있는 항목을 부류별로 세면
  > `Q135976384`(하계 올림픽 에디션) **3건** · `Q137592217`(동계) **3건** ·
  > `Q3327913`(하계 패럴림픽) 2건. 월드컵·아시안게임을 더해도 열몇 건이다.
  > 페이지 하나를 지탱하지 못한다.
  >
  > **② 검산점이 없다.** 개막일을 견줄 두 번째 점을 못 찾았다.
  > · Wikidata 항목에 **회차(`P1545`)도 시리즈(`P179`)도 없다** — 2026 동계 올림픽
  >   (`Q4630399`)은 `P31`·`P580`·`P582`·`P17` 뿐이다. "올림피아드 번호 = (연도−1896)/4+1"
  >   같은 산술 검산을 걸 자료가 없다는 뜻이다.
  > · 개최국 공휴일과도 무관하다 — 저장소 안에 걸 데가 없다.
  > · 남는 것은 구조 검사(4년 주기 · 계절 창 · 기간 길이)뿐인데, 그건 **연도와 달이
  >   틀린 것만 잡고 개막일이 며칠 어긋난 것은 못 잡는다.** 그런데 개막일이 곧 이 자료의
  >   전부다. 황금연휴는 "연휴가 공휴일에 걸려 있나" 라는 의미 있는 제약이 있었고 하늘은
  >   외부 지점이 21개였는데, 여기는 둘 다 없다.
  >
  > **날짜와 이름 자체는 좋았다.** 2030년 동계까지 날짜가 나오고(2030-02-01~02-17)
  > 한국어 이름도 다 있다. 기념일이 걸린 ①한국어 ②큐레이션 문제는 여기 없다.
  > 오직 검산점과 크기에서 걸렸다.
  >
  > **되살리려면** — 별도 페이지가 아니라 **다른 페이지의 한 줄**로 얹는다.
  > 열몇 건은 섹션 하나로는 충분하다. 그때도 검산점 문제는 남으므로, 담는다면
  > "구조 검사만 걸려 있다" 를 페이지에 밝힌다.

- [ ] **국제 기념일** — B급에서 짓기 직전에 내렸다 (2026-08-28)

  > **짐작이 아니라 재 보고 내렸다.** B급 사유가 *"검산점이 약하다"* 였는데, 착수 전에
  > 그것부터 확인했고 예상보다 나빴다. 다시 올라오지 않도록 잰 값을 적어 둔다.
  >
  > **① 저장소 안 검산점 — 21건 중 5건.** 널리 알려진 고정일 기념일을 우리 204개국
  > 공휴일 자료에 걸어 보니 이름까지 맞아떨어지는 것은 다섯뿐이었다.
  > 노동절 141곳 · 국제 여성의 날 20곳 · 아프리카의 날 8곳 · 국제연합일 2곳 · 인권의 날 2곳.
  >
  > **② 그리고 그 다섯은 나머지를 보증하지 못한다. 이게 결정적이다.**
  > 하늘은 *한 이론*이 176건을 다 만들어서 21개 지점이 전체를 검증했다. 기념일 200건은
  > **전부 독립된 사실**이라, "5월 1일이 노동절" 을 맞혀도 "3월 22일이 세계 물의 날" 에
  > 대해서는 아무 말도 하지 못한다. 공유하는 메커니즘이 없으면 표본 검산이 성립하지 않는다.
  >
  > **③ Wikidata — 출처로 못 쓴다.** `Q2558684`(world day) + `P837`(고정일)로 554건이
  > 나오는데,
  > · 한국어 라벨 **166건(30%)** 뿐이고
  > · 큐레이션이 없다 — `World Vegan Burger Day` · `Día Internacional de la Hamburguesa`
  >   (영어 라벨 칸에 스페인어) · `World Parrot Day` 가 UN 기념일과 같은 부류에 섞여 있다
  > · **축이 안 맞는다** — `Father's Day` 가 나라마다 다른 날짜로 8건 중복이다.
  >   전세계 공통축이 아니라 국가축 자료가 섞여 있다는 뜻이다.
  >
  > **④ UN 공식 — 한국어가 아예 없다.** UN 공식 언어는 ar·zh·en·fr·ru·es 여섯이고
  > 한국어는 없다. `un.org/ko/observances/list-days-weeks` 는 **404** 다.
  > 영문 목록 페이지도 JS 렌더라 기계로 읽히지 않는다 (옵서번스 링크가 2~13개만 잡힌다).
  > UNIC 서울(`unic.or.kr`)은 접속 자체가 안 됐다.
  >
  > **⑤ 그래서 ko 페이지의 70%가 영어로 남는다.** 이 사이트는 반대 방향(영어 페이지에
  > 한국어가 새는 것)을 검사로 막고 있는데, 이쪽은 검사조차 없는 종류의 고장이다.
  > 게이트 4번만이 아니라 2번(두 언어를 다 채울 수 있나)에서도 걸린 셈이다.
  >
  > **되살리려면** — 큐레이션된 UN 목록과 **권위 있는 한국어 표기**가 같이 있는 출처가
  > 나와야 한다. 둘 중 하나만으로는 안 된다.

- [ ] **서머타임 전환일** — 한때 A급으로 올렸다가 내렸다 (2026-08-28)

  > **기술적으로는 넷을 다 통과했다.** 그래서 내린 이유를 적어 둔다 — 안 그러면 석 달 뒤에
  > 같은 근거로 다시 올라온다.
  >
  > 통과했던 근거 (Node 24.13 에서 실제로 돌려 본 것):
  > · `new Intl.Locale('und-KR').getTimeZones()` → `['Asia/Seoul']` — 204개국 전부 나온다
  > · `timeZoneName: 'longOffset'` 을 날짜별로 훑으면 전환일이 그대로 떨어진다
  > · 2026 에 전환이 있는 나라 61개. API 도 의존성도 필요 없다 (tzdata 가 Node 에 있다)
  >
  > **내린 이유 — 게이트 1번(미래 날짜가 미리 확정되는가)이 실은 안 통과한다.**
  > 서머타임은 *천문이 아니라 정치가 정하는 날짜*다. 미국은 상원을 몇 번 통과했고,
  > EU 는 2019년에 폐지 지침을 의결해 놓고 아직 시행하지 않았다. 규칙이 바뀌면 tzdata 가
  > 따라오고 생성기는 조용히 "전환 없음" 을 내는데, **그게 맞는 동작이라 아무도 못 알아챈다.**
  > 공휴일은 연 1회 확정되고 바뀌면 diff 에 보이지만, 이쪽은 나라 하나가 통째로 사라져도
  > 표가 비는 것으로만 나타난다.
  >
  > **자료를 담을 값어치보다 시간대 표가 값어치 있었다.** 이 항목을 파다 나온 진짜 소득은
  > `getTimeZones()` 이고, 그건 아래 「기존 것 보강」으로 옮겼다. 그쪽은 정치와 무관하다.
  >
  > **⚠ 사실 확인 메모.** 내릴 때 "미국이 서머타임을 폐지했다" 를 근거로 이야기했는데,
  > 확인해 보니 **Node 24.13 에 들어 있는 tzdata 2025b 는 아직 미국 전환을 담고 있다** —
  > `America/New_York` 2026-03-08 / 11-01, 2027-03-14 / 11-07. (전환이 없는 것은
  > `America/Phoenix` · `Pacific/Honolulu` 처럼 원래 안 하던 곳이다.)
  > 폐지가 확정됐다면 tzdata 를 최신으로 올려 다시 확인할 것. 다만 **내린 판단 자체는
  > 미국과 무관하게 선다** — 61개국 대부분이 유럽이고, 위의 "정치가 정한다" 가 본 이유다.

- [ ] **국가별 선거일**

  > 축은 완벽하다(국가축). 확정된 것도 많다.
  > **3번과 4번에서 같이 걸린다** — 자유롭게 쓸 수 있는 무키 출처가 마땅치 않다.
  > (IFES ElectionGuide 는 키가 필요하다.) Wikidata 로 긁을 수는 있으나 항목마다 품질이
  > 고르지 않고, 받아 적은 것이 맞는지 볼 검산점이 없다. 선거일은 틀리면 곤란한 종류의
  > 자료라 "대충 맞으면 된다" 로 갈 수 없다.

- [ ] **수능·학사일정 등 한국 한정 자료**

  > 검색량은 이 문서의 어떤 후보보다 높다. 그래서 아깝다.
  > **2번에서 걸린다** — 이 사이트의 축은 204개국 × 2개 언어다. KR 페이지에만 붙는 자료는
  > 나머지 203개와 결이 맞지 않고, "왜 한국만 특별한가" 가 구조에 남는다.
  > 굳이 한다면 이 저장소가 아니라 별도 사이트다.

## 화면

이 문서는 자료를 정하는 자리이고 화면은 `README.md` 가 설명한다. 다만 **자료 판단에
영향을 주는 화면 결정 하나**는 여기 적어 둔다.

- [x] **국기를 이모지에서 SVG 로** (2026-09-03)

  > 자료를 하나 더 들인 셈이라 게이트로 재 봤다. 넷 다 통과한다 —
  > 1 확정성(국기 도안은 몇 해에 한 번 바뀐다) · 2 축(국가축, 정확히 맞는다) ·
  > 3 무키(MIT, 키 없음) · 4 검산점(**`countries.json` 과 1:1 이고, 국기가 그 줄의
  > 나라와 같은지까지 되짚는다**).
  >
  > 착수 이유는 아름다움이 아니라 **고장**이었다. 이모지 국기는 윈도우에서 국기로
  > 그려지지 않는다 — 국내 방문자 대부분이 그 화면을 보고 있었다.
  >
  > 무게는 204개 1,406 KB 다. 목록에 `loading="lazy"` 를 걸어 한 페이지가 다 받지
  > 않게 했다. 세르비아 180 KB · 볼리비아 109 KB 는 국장이 든 국기이고,
  > 20×15 에서는 안 보이는 세부인데 줄일 수단이 없다(래스터라이저가 없다) —
  > `gen-flags` 가 100 KB 넘는 것을 찍어 주므로 조용히 이고 있지는 않는다.

- [x] **하늘 표에 그림 달기** (2026-09-04)

  > 국기가 자리를 잡고 나서 하늘 표만 글자뿐이었다. 자료를 하나도 더 들이지 않는다 —
  > **이미 담아서 검증하는 `sky.json` 을 그림으로 다시 읽은 것**이 전부다.
  > 그래서 게이트 넷은 재지 않았고(밖에서 받아오는 것이 없다), 대신 화면 쪽 판단이
  > 하나 있었다: 72줄에 같은 그림을 72번 찍을 것인가.
  >
  > 아니라고 봤다. 절기는 황경 `k` 가 곧 원 위의 각도이고, 삭·보름은 빈 원과 찬 원,
  > 유성우는 ZHR 이 줄기 수다. 표를 내려가면 해가 원을 한 바퀴 돈다. 29개 7 KB.
  >
  > **음력에는 안 달았다.** 초하루가 곧 삭이라 삭 그림을 그대로 쓸 수 있는데, 그러면
  > 같은 그림이 두 갈래에서 다른 뜻으로 읽힌다 — 저쪽 표의 한 행은 순간이 아니라 달이다.
  >
  > 검산점은 파비콘·카드와 같은 자리다 — **그린 것을 다시 읽어서 본다.** 그 덕에
  > 유성우 줄기 둘이 같은 선 위에 앉아 한 줄로 보이던 것을 화면 아닌 곳에서 잡았다.

- [x] **요일 축 — 공휴일 요일 분포** `/weekday/` (2026-09-04)

  > **밖에서 받아온 것이 하나도 없고 `data/` 에 파일도 하나도 더 만들지 않았다.**
  > 이미 담아 검증하는 204개국 공휴일을 **요일**로 다시 묶은 것이 전부다 —
  > 이름 축(2026-09-03)과 같은 방식이고, 페이지가 544 → 546 (한 장 × 2언어).
  > 축 탭은 넷 그대로다. 물음이 "나라끼리 견주기" 와 같아서 `/rank/` 의 둘째 장으로 뒀다.
  >
  > **게이트 4번(검산점)이 이름 축과 같은 방식으로 성립한다** — `check-pages` 가
  > 자기 손으로 다시 세어 견주고, 묶는 코드를 나눠 쓰지 않는다.
  >
  > **잰 값** (표지 연도 2026, 쉬는 날짜 2,500건 · 204개국):
  > 금 617(24.7%) · 월 520 · 목 451 · 토 288 · 일 252 · 화 211 · **수 161**(6.4%).
  > 고른 분포라면 각 14.3% 인데 **금요일이 수요일의 3.8배**다.
  >
  > **⚠ 쏠림의 원인을 처음에 틀리게 봤다.** 부활절 계열이라고 적었는데 자료를 세어
  > 보니 **고정 날짜의 동기화**가 절반이었다 — 2026-01-01(목)이 197개국,
  > 2026-12-25(금)이 178개국이다. 목요일 451건의 43.7%, 금요일 617건의 28.8% 다.
  > 부활절 계열은 그 위에 얹힌다.
  >
  > **⚠⚠ 한 번 기각한 지표를 되살렸다 — README 가 그걸 막아 줬다.** "주말에 겹쳐
  > 날아간 공휴일" 은 *"토·일 고정으로 세면 이집트(금·토)에서 조용히 틀린다"* 는
  > 이유로 뺀 지표였는데, 그걸 모르고 그대로 넣고 있었다(540 · 21.6% — 기각 당시와
  > 같은 숫자였다). 문서가 잡아 준 것이다.
  >
  > **그리고 그 자료가 `Intl` 에 있었다.** `Intl.Locale('und-EG').getWeekInfo().weekend`
  > 가 `[5,6]` 을 주고 204개국 전부 잡힌다 — 파일도 네트워크도 안 늘어난다.
  > 나라별 주말로 세니 **540 이 아니라 544(21.8%)** 다. 네 건이 그 차이다.
  > 토·일 195개국 · 금·토 8개국 · **일요일 하루뿐 1개국(인도)**.
  > 검사가 "전 세계 합계가 토·일 칸의 합과 같으면 실패" 로 그 가정을 붙든다.
  >
  > **주말 겹침이 해마다 두 배로 흔들린다** — 2025년 16.6% · 2026년 21.8% ·
  > 2027년 **31.5%**. 2027년에 크리스마스가 토요일이라 160개국이 같은 해에 함께
  > 손해를 본다. 나라별로 흩어지지 않고 전 세계가 동시에 겪는 것이 이 표의 요점이다.
  >
  > **이름이 걸치는 요일 수**를 이름 축의 `NORM`·`NAMES` 로 센다. 1이면 요일이
  > 정의에 박힌 것(성금요일 128건 전부 금요일), 7이면 나라마다 날짜가 다른 것
  > (독립기념일 106건). 새해가 2개 요일, 크리스마스가 5개 요일인 것도 그 표에서 보인다.
  >
  > 변이 넷을 넣어 무는 것도 확인했다 — 요일 한 칸 바꾸기 · 표의 한 줄을 다른 나라로 ·
  > **세는 단위를 건수로 바꾸기(40건 실패)** · 표 스타일 지우기.

- [x] **다른 달력의 새해** `/sky/calendar/` (2026-09-04)

  > **밖에서 받아온 것이 하나도 없고 `data/` 에 파일도 하나도 더 만들지 않았다.**
  > 자료가 런타임의 국제화 표(ICU/CLDR) 안에 이미 있었다 —
  > `Intl.DateTimeFormat('en-u-ca-hebrew')` 다. 키도, 네트워크도, 의존성도 없다.
  > `sky.json` 에 `cals` 키 하나(21행 · 1.2KB)가 늘고 페이지가 546 → 548 이다.
  > 하늘의 다섯째 갈래로 붙였다 — 새 축도, 새 탭도 아니다.
  >
  > **「안을 한 번 더 뒤집어 볼 것」의 다음 판이다.** 이름 축·요일 축은 우리가 담은
  > 자료를 뒤집었고, 이번엔 **런타임이 이미 들고 있는 표**를 뒤집었다. 손으로 적는
  > 것은 어느 달력을 실을지와 그 이름뿐이고(`tools/calendars.mjs`), 날짜 계산은
  > 한 줄도 없다 — 국제 기념일이 탈락한 이유(*사실 자체를 손으로 적어야 한다*)의
  > 반대편이다.
  >
  > **페이지의 축은 "새해를 무엇이 정하는가" 다.** 천문이 정하는 일곱(음력·히즈라·
  > 히브리·페르시아·인도 사카·에티오피아·콥트)은 서력과의 차이가 해 안에서 바뀌고,
  > 연호만 바꾼 셋(불기·민국·일본 연호)은 **차이가 상수**다(−543 · +1911 · +2018).
  > 2026년 1/1 과 12/31 을 나란히 놓으면 그 갈래가 눈에 보인다.
  >
  > **게이트 4번(검산점)이 가장 세게 성립한 자리다.** 받아 적은 자료라 "받은 대로
  > 찍혔나" 만으로는 아무것도 검사되지 않아서, 서로 다른 곳에서 온 질문 여섯을 던진다.
  > 그중 값나가는 것이 **우리 음력 ↔ ICU 단기력**이다 — 이 저장소는 음력을 직접
  > 계산하고(Meeus 49장 + VSOP87D) ICU 는 완전히 다른 구현인데, **37개월 초하루가
  > 전부 일치**한다. 설날만 견주면 세 번이지만 초하루를 다 견주면 서른일곱 번이다.
  > 노루즈는 `sky-fixture.mjs` 의 손으로 적은 공표 분점 시각 + 테헤란 정오 규칙으로
  > 또 한 번 견준다 — 우리 계산도 ICU 도 모르는 제3의 값이다.
  >
  > **⚠ 검사 창이 자료 창보다 좁아 반쪽이 됐다.** 노루즈 규칙은 "테헤란 정오 이전/
  > 이후" 두 갈래인데 자료의 3년이 셋 다 정오 *이후*라 다른 갈래가 한 번도 안 걸렸다.
  > 주석에 *"두 갈래가 다 걸려야 검사다"* 라고 써 놓고 반쪽이었다. **자료는 3년,
  > 검사는 31년**으로 갈랐다 — 계산이 순수 함수라 공짜다. 이제 16해·15해로 갈리고,
  > 한쪽이 0 이면 그 자체로 실패한다. **검사 범위를 자료 범위에 맞출 이유가 없다.**
  >
  > **⚠ 표 머리 글자가 어느 갈래에서도 검사되지 않고 있었다** — 칸수만 셌다. 그래서
  > 시각이 없는 달력 표에 "날짜와 시각 / 천문 현상" 이라고 적혀 있어도 통과했다.
  > 이번에 글자까지 견주게 했고, 갈래가 머리를 갈아 끼우는 것을 잊으면 걸린다.
  >
  > 간지 표기는 **언어를 물어서** 받는다 — ICU 가 `en` 에 `bing-wu`, `ko` 에 `병오` 를
  > 준다. 처음에 `en` 만 물어서 한국어 페이지에 `bing-wu` 가 찍혔다. 간지를 우리가
  > 세지 않고 물어보는 값이니 표기도 물어봐야 짝이 맞는다. 단기력은 ICU 에서 해에
  > **번호가 없어서**(`year` 파트가 안 나온다) 그 칸을 *번호가 없다 (간지)* 로 비운다.
  >
  > 변이 여섯을 넣어 무는 것도 확인했다 — 노루즈 하루 밀기(4곳에서 걸린다) ·
  > 해 길이 하루 늘리기 · 설날 하루 밀기 · 간지 한 언어만 지우기 ·
  > 표 머리 갈아끼우기 잊기 · 연호 차이 1911→1912(**빌드가 멈춘다**).

## 기존 것 보강

- [ ] **「알려진 제약」 1번 — D-day 가 보는 사람의 기기 날짜다.** ← **가장 값어치 있는 한 건**

  다른 시간대의 국가를 볼 때 하루 어긋난다. README 에는 *"국가별 대표 시간대를 담으면 고칠
  수 있지만 CLDR territory→timezone 표를 들여와야 해서 지금은 각주로만 적어 두었다"* 고
  썼는데, **그 표가 `Intl.Locale.getTimeZones()` 로 이미 있다.** Node 24.13 에서 204개국
  전부 나오는 것을 확인했다 — `new Intl.Locale('und-KR').getTimeZones()` → `['Asia/Seoul']`.
  들여올 자료도, 의존성도 없다.

  국가 페이지의 "오늘" 을 기기 날짜가 아니라 그 나라 시간대의 날짜로 정하면 된다.
  자료가 아니라 계산을 고치는 일이라 `data/` 는 안 커진다.

  **정하고 들어갈 것 하나** — **26개국이 시간대를 둘 이상 갖는다** (미국은 4개 이상).
  대표 하나로 뭉개면 미국 페이지가 틀린다. 대표 시간대 + "이 나라는 시간대가 N개" 각주로
  가거나, 여러 줄로 내거나 둘 중 하나를 먼저 정해야 한다.

  서머타임 항목은 내렸지만(위 「탈락」) **이건 그것과 무관하게 선다** — 시계를 옮기든 말든
  나라마다 지금 날짜가 다른 것은 그대로다.

- [ ] **`check-pages.mjs` 가 국가 하나를 두 번 검사한다.**
  자료 정합성 검사가 페이지 순회 안에 있어서 ko/en 두 번 돈다. 값싼 검사라 일부러 그렇게
  두었지만(두 페이지가 같은 자료를 봤다는 뜻이 된다), 자료 검사가 더 무거워지면 밖으로 뺀다.

## 자료를 하나 더 붙일 때 손대는 곳

황금연휴와 천문에서 실제로 건드린 곳 전부다. 하나라도 빠지면 조용히 반쪽이 된다.

- [ ] **축이 셋으로 늘었다 — 국가 · 전세계 공통 · 이름.** 이름 축처럼 **자료가 페이지
  수를 정하는** 축이면 슬러그를 `EXTRA` 에 손으로 적지 말 것. `config.mjs` 의
  `NAME_PAGE` 처럼 모양으로 걸고 `kindOf` 에 갈래를 하나 더한다. 손으로 적으면 자료가
  바뀔 때 조용히 어긋난다. 뿌리(`holiday`)만 `EXTRA` 에 있으면 청소가 하위까지 지운다.
- [ ] **국가 축인가 공통 축인가부터 정한다.** 공통 축이면 `config.mjs` 의 `EXTRA` 에
      슬러그를 넣고(두 글자 금지 — 국가 코드와 부딪힌다) `check-pages` 의 갈래를 늘린다.
- [ ] **받아 오는 자료면 `gen-holidays.mjs`** — 받고, 거르고, **버린 건수를 찍는다.**
      조용히 사라지면 다음 달에 왜 줄었는지 알 수 없다.
      **계산하는 자료면 `gen-sky.mjs` 처럼 따로 둔다** — 네트워크 단계와 섞지 않는다.
- [ ] **검산점을 정한다** — 게이트 4번. 황금연휴의 "공휴일에 걸려 있나" 에 해당하는 것.
- [ ] **`gen-pages.mjs` 의 `L.ko` / `L.en`** — 페이지 문안 두 벌
- [ ] **`dday.js` 의 `STR.ko` / `STR.en`** — 화면에서 바뀌는 문안 두 벌
- [ ] **자료가 낡으면 멈추게 한다.** `gen-pages` 가 `data.long` 이 없으면 종료 코드 1 을
      내는 것처럼. 섹션 하나가 조용히 빠지는 게 가장 알아채기 어렵다.
- [ ] **`harness.mjs`** — 새 행 모양을 파싱해 준다. 안 하면 `check-pages` 가 "그려진 결과" 를
      되읽지 못하고 HTML 문자열만 보게 된다.
- [ ] **`check-pages.mjs` 의 기대값을 따로 적는다.** `dday.js` 의 함수를 가져다 쓰면
      아무것도 검사하지 않는 것과 같다 — 번역이 통째로 틀려도 통과한다.
- [ ] **CSS 계약을 `check-pages` 에 박는다.** 화면으로만 확인되는 것은 하니스가 못 본다.
- [ ] **표 머리 글자까지 검사한다.** 칸수만 세면 시각이 없는 표에 "날짜와 시각" 이라고
      적혀 있어도 통과한다 (`/sky/calendar/` 에서 걸린 자리다).
- [ ] **검사 범위를 자료 범위에 맞추지 않는다.** 규칙에 갈래가 둘이면 **두 갈래가 다
      걸리는 범위**까지 넓힌다. 계산이 순수 함수면 공짜다 — 노루즈는 자료 3년 ·
      검사 31년이고, 한 갈래가 0 번 걸리면 그 자체로 실패하게 해 둔다.
- [ ] **영어 누출.** 누출 검사는 `<td class="name">` 을 면제한다(공휴일 이름은 현지어라서).
      **우리가 쓰는 말은 그 클래스를 쓰면 안 된다** — 황금연휴 칸을 `class="len"` 으로 둔 이유다.
- [ ] **일부러 깨뜨려 검사가 무는지 본다.** 자료 한 칸 밀기 · 규칙 위반 심기 · CSS 규칙 지우기 ·
      계산 1 틀리기. 안 물면 그 검사는 없는 것이다.
- [ ] **배포 순서** — `gen-holidays` → `gen-globe` → `gen-pages` → **commit** → `gen-sitemap` → `check-pages` → push.
      commit 이 먼저여야 `lastmod` 가 맞는다. `sitemap.xml` 은 `--amend` 로 같은 커밋에 합친다.
      `gen-globe` 는 `countries.json` 이 바뀔 때만 돌리면 된다 — 나라가 늘면 점이 없다며 멈춘다.
