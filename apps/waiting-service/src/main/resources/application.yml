server:
  port: 0
  shutdown: graceful

spring:
  application:
    name: waiting-service

  threads:
    virtual:
      enabled: true

  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
    show-sql: false

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: waiting-service-group-out
      auto-offset-reset: latest
management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, prometheus
  endpoint:
    prometheus:
      enabled: true
    health:
      show-details: always
    metrics:
      tags:
        application: ${spring.application.name} # 여러 서버 식별을 위한 태그

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    prefer-ip-address: true


logging:
  level:
    # 👇 Kafka 설정값(AbstractConfig) 로그만 쏙 골라서 WARN 이상일 때만 출력하게 변경
    org.apache.kafka.common.config: WARN

    # (선택) Kafka 클라이언트 전체 로그를 좀 조용히 시키고 싶다면
    org.apache.kafka.clients.producer: WARN
    org.apache.kafka.clients.consumer: WARN

app:
  kafka:
    topics:
      waiting-events: waiting-events
  feign:
    restaurant:
      url: "http://localhost:6000/restaurants"


resilience4j:
  bulkhead:
    instances:
      restaurantService: # [중요] 이 이름이 나중에 어노테이션의 name과 일치해야 합니다.
        maxConcurrentCalls: 20  # 동시에 20개 스레드까지만 진입 허용
        maxWaitDuration: 100ms  # 꽉 찼을 때 0.1초만 대기하고 바로 거절(Fast Fail)

  # (선택) 서킷 브레이커 설정도 오버라이딩하고 싶다면 추가
  circuitbreaker:
    instances:
      restaurantService:
        baseConfig: default # 공통 모듈의 설정을 상속받음
        slidingWindowSize: 20 # 식당 호출은 빈번하지 않으니 윈도우 사이즈를 작게 조정