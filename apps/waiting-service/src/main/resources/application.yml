server:
  port: 8080

spring:
  application:
    name: waiting-service

  # [DB ??] H2 In-Memory DB ??
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL # PostgreSQL ??? ?? (?? ??? ??)
    username: sa
    password:

  # [JPA ??]
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update # ???(Java)? ?? ???? ???? ??/??
    show-sql: true      # ???? SQL? ??? ??? (???)
    properties:
      hibernate:
        format_sql: true # SQL ??? ???

  # [H2 ?? ???] ?????? DB ?? ??
  h2:
    console:
      enabled: true
      path: /h2-console