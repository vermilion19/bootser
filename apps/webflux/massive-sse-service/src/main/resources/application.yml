spring:
  application:
    name: massive-sse-service
  data:
    redis:
      host: localhost
      port: 6379

app:
  broadcaster:
    type: memory  # 'memory'로 바꾸면 Redis 없이 동작, 'redis'로 하면 Redis 필요

