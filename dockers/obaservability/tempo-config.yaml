server:
  http_listen_port: 3200

distributor:
  receivers:
    zipkin: # 스프링 부트의 zipkin-reporter가 여기로 쏩니다.
      endpoint: "0.0.0.0:9411"

storage:
  trace:
    backend: local
    local:
      path: /tmp/tempo/blocks