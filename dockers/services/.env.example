# ==============================================
# Booster Docker Environment Variables
#
# 사용법:
# 1. 이 파일을 복사: cp .env.example .env
# 2. .env 파일의 값들을 수정
# 3. docker-compose up -d 실행
# ==============================================

# PostgreSQL (필수)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here

# JWT Secret (필수 - auth-service용)
# 생성 명령어: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_here

# Slack Webhook (선택 - notification-service용)
SLACK_WEBHOOK_URL=