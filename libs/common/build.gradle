dependencies {

    // 1. Jackson (JSON 직렬화/역직렬화)
    api 'org.springframework.boot:spring-boot-starter-json'

    // 2. Apache Commons (강력한 String, Collection 유틸)
    // StringUtils.isEmpty() 등을 쓰기 위해 추가
    api 'org.apache.commons:commons-lang3'
}
